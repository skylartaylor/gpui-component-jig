name: "Registry Release (Disabled)"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  registry_release_disabled:
    name: Registry release is intentionally disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use an advertised immutable Git ref; crates.io publishing requires a separate packaging and versioning decision."
